/**
 * This module is consumed by accessing the js and css resources in dist folder.
 *
 * To get the root path to the content, you can use:
 * const uiRoot = path.dirname(require.resolve('@sap/guided-answers-extension-webapp'));
 *
 * From there, index is at path.join(uiRoot,'index.js') or css is at
 * path.join(uiRoot,'guidedAnswers.css')
 */
